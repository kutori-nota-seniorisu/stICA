设置一次运行中，最大调用函数次数与最大迭代次数分别为500000和28800.

对于数据集8,9，在一次运行中可以得到最优化结果，算法终止信息为：梯度小于容差。

对于数据集7,10，(1)在一次运行中无法得到最优化结果，会频繁出现skipped update信息。(2)如果设置最大调用函数次数为100000，则需要多次运行算法，算法终止信息为：步长小于容差。(3)若再设置步长容差为eps，那么算法终止信息为：无法使目标函数沿当前方向下降。而这些停止条件的可信度并不高。而且在(1)的条件下，虽然出现skipped update信息，但是函数值仍然在下降，且比(2)和(3)算法停止时的函数值要更小。
为什么会出现这种问题？初步排查后得知，采用max-min归一化会使NMF结果不满秩，从而导致优化失效。采用按列归一化后，基本不会出现NMF不满秩的情况，优化算法运行有效。

2025.09.08
α=0.5，拟合用Xt，set1,3,4停止时为fminunc 已停止，因为当前步长小于步长容差的值。

2025.09.09
修改步长容差为eps，set1,3能迭代至梯度小于容差后结束，找到局部最小值。set4无法递减。
α=0.5，拟合用Xt，set4停止时为fminunc 已停止，因为它无法使目标函数沿当前搜索方向递减。
α=0.1，拟合用sech与Xt，set1停止时为fminunc 已停止，因为它无法使目标函数沿当前搜索方向递减。

2025.09.11
增加了二次迭代，若无法使目标函数沿当前搜索方向递减，则调整为中心差分进行二次迭代。
α=0.5，拟合用Xt，set4找到局部最小值。
α=0.1，sech与Xt仍然有问题：优化已停止，因为无法使目标函数沿当前搜索方向递减。原因是目标函数中的预测变化，或线搜索间隔 eps。